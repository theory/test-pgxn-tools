name: 🧪 Test PGXN Bundle
on:
  push:
jobs:
  bundle:
    name: Bundle
    runs-on: ubuntu-latest
    container: pgxn/pgxn-tools:_testing_do_not_use
    steps:
    - uses: actions/checkout@v2
    # - run: pgxn-bundle
    - run: git archive -v --format zip --prefix foo/ --output foo.zip HEAD
