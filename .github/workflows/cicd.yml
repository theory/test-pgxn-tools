name: 🧪 Test PGXN Bundle
on:
  push:
jobs:
  bundle:
    name: Bundle
    runs-on: ubuntu-latest
    container: pgxn/pgxn-tools:_testing_do_not_use
    steps:
    - uses: actions/checkout@v4
    # - run: pgxn-bundle
    - run: cat /tmp/entrypoint.txt
    # - run: git rev-parse --is-inside-work-tree || true
    # - run: git config safe.directory
