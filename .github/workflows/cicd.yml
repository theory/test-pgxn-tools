name: Test Git
on:
  push:
jobs:
  bundle:
    name: Git
    runs-on: ubuntu-latest
    container:
      image: alpine
      options: "--user nobody"
    steps:
    - uses: actions/checkout@v4
    - run: ls -lah
